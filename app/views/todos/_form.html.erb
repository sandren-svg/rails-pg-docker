<%= form_with(model: todo, local: true) do |form| %>
  <% if todo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo.errors.count, "error") %> prohibited this todo from being saved:</h2>

      <ul>
      <% todo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :todo_name %>
    <%= form.text_field :todo_name %>
  </div>

  <div class="field">
    <%= form.label :todo_description %>
    <%= form.text_field :todo_description %>
  </div>

  <div class="field">
    <%= form.label :is_done %>
    <%= form.text_field :is_done %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
